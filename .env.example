# UI Configuration
VITE_USE_DYNAMIC_UI=true

# Backend API Key (server-side only, never exposed to browser)
# API_KEY=your_backend_api_key_here

# Backend API URL (used by BFF proxy)
# API_BASE_URL=http://localhost:8000
